export async function getUser(email: string, password: string) {
  // Ovde ide logika za proveru korisnika u bazi
  // Primer:
  // const user = await db.user.findUnique({ where: { email } });
  // if (user && user.password === password) return user;
  // return null;
}